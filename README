# Clipping Software

A high-performance screen clipping tool with a modern GUI.

## Requirements
- Python 3.8+
- FFmpeg installed and added to PATH.

## Installation
1. Clone this repository.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt